__pycache__/
*.pyc
.idea/
.selfhost-data/
.selfhost-runtime/
.preview_server.py

# Retired pre-V2 local data. V2 never imports or loads these directories.
tasks/
agent-runtime/
memory/
agent-profiles.json

# V2 local projects, sessions, provider credentials, and events.
workbench/

# PyInstaller build artifacts and local agent session data.
build/
dist/
.zcode/

# Local UI-verification output: headless browser profiles, screenshots, and
# scratch server logs.  Large and machine-specific; never source.
artifacts/

# zvec-grep keeps its local index beside the project it scans.
.zvec-grep/
